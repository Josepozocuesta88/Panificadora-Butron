name: Deploy Florys to Hostinger

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Verify secrets
      run: |
        echo "FTP Server: ${{ secrets.HOSTINGER_FTP_SERVER }}"
        echo "FTP Username: ${{ secrets.HOSTINGER_FTP_USERNAME }}"
        echo "FTP Port: ${{ secrets.HOSTINGER_FTP_PORT || 21 }}"

    - name: List local directory
      run: ls -R ./

    - name: Deploy via FTP
      uses: SamKirkland/FTP-Deploy-Action@v4
      with:
        server: ${{ secrets.HOSTINGER_FTP_SERVER }}
        username: ${{ secrets.HOSTINGER_FTP_USERNAME }}
        password: ${{ secrets.HOSTINGER_FTP_PASSWORD }}
        port: ${{ secrets.HOSTINGER_FTP_PORT || 21 }}
        local-dir: ./
        server-dir: /public_html
        exclude: |
          .git*
          node_modules/
          tests/
          vendor/
          .env
          storage/
          .github/
        exclude-older: true

    - name: Upload Vendor Folder
      uses: SamKirkland/FTP-Deploy-Action@v4
      with:
        server: ${{ secrets.HOSTINGER_FTP_SERVER }}
        username: ${{ secrets.HOSTINGER_FTP_USERNAME }}
        password: ${{ secrets.HOSTINGER_FTP_PASSWORD }}
        port: ${{ secrets.HOSTINGER_FTP_PORT || 21 }}
        local-dir: ./vendor
        server-dir: /public_html/vendor
        exclude-older: true

    - name: Upload Storage Folder
      uses: SamKirkland/FTP-Deploy-Action@v4
      with:
        server: ${{ secrets.HOSTINGER_FTP_SERVER }}
        username: ${{ secrets.HOSTINGER_FTP_USERNAME }}
        password: ${{ secrets.HOSTINGER_FTP_PASSWORD }}
        port: ${{ secrets.HOSTINGER_FTP_PORT || 21 }}
        local-dir: ./storage
        server-dir: /public_html/storage
        exclude-older: true